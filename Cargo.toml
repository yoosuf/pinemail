[workspace]
resolver = "2"
members = ["crates/core", "crates/server", "crates/mcp"]

[workspace.package]
version = "0.1.0"
edition = "2021"
license = "MIT"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
